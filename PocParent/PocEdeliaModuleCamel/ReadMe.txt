Camel Router WAR Project
========================

This project includes a sample route as as a WAR.
You can build the WAR by running

    mvn install

You can then run the project by dropping the WAR into your 
favorite web container or just run

    mvn jetty:run

to start up and deploy to Jetty.

les Web service
Les libs des Web Services sont génerées par maven (répertoire : /generated/cxf).
Puis elles sontn utilisée dans les endpoint des routes proxy
  



TODO
-- Mettre les details de la base postgrel dans le fichier de configuration
-- Refactor les junit test
 
       
       